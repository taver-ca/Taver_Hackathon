{
    "navigationFallback": {
        "rewrite": "/",
        "exclude": [
            "*.{css,scss,js,png,gif,ico,jpg,svg}"
        ]
    },
    "routes": [
        {
            "route": "/ShowSpotifyArtists",
            "rewrite": "/"
        }
    ]
}